# Example file for setting the necessary environment variables.
# Set the variables accordingly and rename the file to '.env'

# devaddr in little endian format
TARGET_IP="{{hostvars['ttnstack']['ansible_host']}}"
TARGET_PORT={{ target_port }}
GATEWAY_EUI="{{ gateway_eui }}"
NWKSKEY="{{ nwk_s_enc_key }}"
APPSKEY="{{ app_s_key }}"
DEVADDR="{{ dev_addr_le }}"

